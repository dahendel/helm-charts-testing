apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-demo.fullname" . }}-test-connection"
  labels:
{{ include "api-demo.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "api-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
