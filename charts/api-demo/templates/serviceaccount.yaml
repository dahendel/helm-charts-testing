{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "api-demo.serviceAccountName" . }}
  labels:
{{ include "api-demo.labels" . | indent 4 }}
{{- end -}}
